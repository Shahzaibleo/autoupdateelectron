{
  "name": "autoupdatetest",
  "version": "1.0.1",
  "description": "testing",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "electron-builder build --win --publish always",
    "deploy": "electron-builder build --win --publish always"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Shahzaibleo/autoupdateelectron.git"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^20.1.1",
    "electron-builder": "^23.3.3"
  },
  "dependencies": {
    "deploy": "^1.0.3",
    "electron-updater": "^5.2.1"
  }
}
